{
  "id": "repair.poisson_blend@1",
  "impl_version": 1,
  "summary": "Gradient-domain seamless cloning: paste a source region into a target image so the masked interior reproduces the source's gradients (Δu = Δsource) while matching the target on the mask boundary (Dirichlet continuity), solved per channel by row-major Gauss-Seidel. Deterministic (bit-identical reruns); the report carries SolverData (iterations, residual history, stop reason).",
  "determinism": "bounded",
  "roi": {
    "category": "full-domain"
  },
  "inputs": [
    {
      "name": "source",
      "kind": "Image",
      "required": true,
      "doc": "The image whose gradients are cloned inside the mask."
    },
    {
      "name": "target",
      "kind": "Image",
      "required": true,
      "doc": "The image the source is blended into; kept outside the mask and used as the Dirichlet boundary."
    },
    {
      "name": "mask",
      "kind": "Mask",
      "required": true,
      "doc": "The coverage mask selecting the interior region to reconstruct (> 0.5 is interior)."
    }
  ],
  "outputs": [
    {
      "name": "candidate",
      "kind": "Image",
      "doc": "The seamlessly-blended image (the target descriptor; same extent/layout)."
    },
    {
      "name": "report",
      "kind": "Report",
      "doc": "The solver report carrying SolverData (iterations, residual history, stop reason, tolerance, final residual)."
    }
  ],
  "params": [
    {
      "name": "max_iterations",
      "type": "integer",
      "required": false,
      "default": 2000,
      "doc": "The maximum Gauss-Seidel sweeps before the solver stops at the cap, in 1..=100000."
    },
    {
      "name": "tolerance",
      "type": "float",
      "required": false,
      "default": 1e-6,
      "doc": "The relative-residual convergence tolerance in (0, 1)."
    }
  ],
  "implementations": [
    "cpu.reference@1"
  ],
  "test": {
    "has_analytic_reference": true,
    "has_property_tests": true,
    "verification": {
      "build-hygiene": {
        "status": "covered"
      },
      "schema-contract": {
        "status": "covered"
      },
      "analytic-fixtures": {
        "status": "covered"
      },
      "property-tests": {
        "status": "covered"
      },
      "metamorphic": {
        "status": "covered"
      },
      "goldens": {
        "status": "covered"
      },
      "perceptual": {
        "status": "not-applicable",
        "reason": "repair.poisson_blend is a gradient-domain Poisson solver verified by analytic fixtures (gradients reproduced inside the mask, target continuity at the boundary), bit-identical reruns, and a residual-history convergence record; there is no perceptual-quality metric to apply"
      },
      "fuzzing": {
        "status": "covered"
      },
      "performance": {
        "status": "covered"
      }
    }
  }
}
