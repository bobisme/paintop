{
  "id": "paint.gaussian_splats@1",
  "impl_version": 1,
  "summary": "Paint an inline batch of anisotropic Gaussian splats (center, sigma, angle, color, opacity, blend) onto a premultiplied linear base image, accumulated in array order; the batch is bounded by policy.resources.max_splats.",
  "determinism": "bounded",
  "roi": {
    "category": "pointwise"
  },
  "inputs": [
    {
      "name": "base",
      "kind": "Image",
      "required": true,
      "doc": "The premultiplied linear-light base image the splats paint onto."
    }
  ],
  "outputs": [
    {
      "name": "image",
      "kind": "Image",
      "doc": "The base image with the splat batch painted on (same extent/layout)."
    }
  ],
  "params": [
    {
      "name": "splats",
      "type": "json",
      "required": true,
      "doc": "The inline batch of splat objects { center_px, sigma_px, angle_rad?, color, opacity?, blend?, hardness? }; accumulated in array order. May be empty. `hardness` in [0,1] (default 0) shapes the falloff: 0 is the pure Gaussian, higher values flatten the core and tighten the edge."
    },
    {
      "name": "space",
      "type": "string",
      "required": false,
      "choices": [
        "linear-srgb",
        "raw-linear"
      ],
      "doc": "Optional paint color space; when present must name the base's linear encoding."
    },
    {
      "name": "max_splats",
      "type": "integer",
      "required": false,
      "default": 100000,
      "doc": "The inline-batch bound from policy.resources.max_splats; a larger batch is rejected with a policy error."
    }
  ],
  "implementations": [
    "cpu.reference@1"
  ],
  "test": {
    "has_analytic_reference": true,
    "has_property_tests": true,
    "verification": {
      "build-hygiene": {
        "status": "covered"
      },
      "schema-contract": {
        "status": "covered"
      },
      "analytic-fixtures": {
        "status": "covered"
      },
      "property-tests": {
        "status": "covered"
      },
      "metamorphic": {
        "status": "covered"
      },
      "goldens": {
        "status": "covered"
      },
      "perceptual": {
        "status": "not-applicable",
        "reason": "paint.gaussian_splats accumulates a closed-form Gaussian field verified by center symmetry, covariance-axis alignment, translation covariance, zero-opacity identity, batch-order/blend semantics, and the inline-batch budget; there is no perceptual-quality metric to apply"
      },
      "fuzzing": {
        "status": "covered"
      },
      "performance": {
        "status": "covered"
      }
    }
  }
}
